import 'package:flutter/material.dart';

class VehiclesDetailView extends StatelessWidget {
  const VehiclesDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}